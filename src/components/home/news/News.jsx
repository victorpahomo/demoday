import Carousel from "./Carousel";

const News = ({ group }) => {
  return (
    <section id="studentNews" className=" bg-white py-5 rounded-2xl border-4 w-full">
      <Carousel />
    </section>
  );
};

export default News;
